---
import type { Media } from '../lib/db/media';

interface Props {
  ownerType: 'berita' | 'wisata' | 'umkm' | 'produk';
  ownerId: number;
  mediaList: Media[];
  purgePaths: string[];
  defaultAlt: string;
  title?: string;
  subtitle?: string;
  buttonLabel?: string;
}
const { ownerType, ownerId, mediaList, purgePaths, defaultAlt, title, subtitle, buttonLabel } = Astro.props;
const displayTitle = title ?? 'Foto Sampul & Galeri';
const displaySubtitle = subtitle ?? 'Foto pertama otomatis menjadi sampul. Gunakan "Jadikan Sampul" untuk mengganti.';
const displayButton = buttonLabel ?? '+ Tambah Foto';
const back = Astro.url.pathname;
const purge = purgePaths.join(',');
const inputId = `foto-input-${ownerType}-${ownerId}`;
const statusId = `foto-status-${ownerType}-${ownerId}`;
---

<div class="mt-8 bg-white rounded-xl border border-border p-6" style="box-shadow:var(--shadow-sm);" data-upload-manager>
  <h3 class="font-heading font-semibold text-lg mb-1" style="color:var(--color-primary);">{displayTitle}</h3>
  <p class="text-xs text-text-muted mb-4">{displaySubtitle}</p>

  {mediaList.length > 0 && (
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3 mb-4">
      {mediaList.map((m, i) => (
        <div class="rounded-lg border border-border overflow-hidden">
          <div class="aspect-square bg-sand/30 relative">
            <img src={`/api/media/${encodeURIComponent(m.r2_key_thumb)}`} alt={m.alt} class="w-full h-full object-cover" loading="lazy" />
            {i === 0 && (
              <span class="absolute top-1 left-1 px-1.5 py-0.5 rounded text-[10px] font-heading font-semibold text-white" style="background:var(--color-cta);">Sampul</span>
            )}
          </div>
          <div class="flex gap-1 p-1.5">
            {i !== 0 && (
              <form method="POST" action="/api/admin/media/manage">
                <input type="hidden" name="_action" value="cover" />
                <input type="hidden" name="media_id" value={m.id} />
                <input type="hidden" name="owner_type" value={ownerType} />
                <input type="hidden" name="owner_id" value={ownerId} />
                <input type="hidden" name="back" value={back} />
                <input type="hidden" name="purge" value={purge} />
                <button type="submit" class="px-1.5 py-1 rounded text-[11px] font-heading border cursor-pointer hover:bg-sand" style="border-color:var(--color-border);">
                  Jadikan Sampul
                </button>
              </form>
            )}
            <form method="POST" action="/api/admin/media/manage" onsubmit="return confirm('Hapus foto ini?')">
              <input type="hidden" name="_action" value="delete" />
              <input type="hidden" name="media_id" value={m.id} />
              <input type="hidden" name="back" value={back} />
              <input type="hidden" name="purge" value={purge} />
              <button type="submit" class="px-1.5 py-1 rounded text-[11px] font-heading border cursor-pointer hover:bg-red-50 hover:text-red-600" style="border-color:#FECACA; color:#DC2626;">
                Hapus
              </button>
            </form>
          </div>
        </div>
      ))}
    </div>
  )}

  <label class="inline-flex items-center gap-2 px-4 py-2.5 rounded-lg font-heading font-medium text-sm border cursor-pointer hover:bg-sand" style="border-color:var(--color-border); min-height:44px;">
    {displayButton}
    <input type="file" accept="image/*" multiple class="hidden" id={inputId} data-upload-manager data-owner-type={ownerType} data-owner-id={ownerId} data-purge={purge} data-default-alt={defaultAlt} />
  </label>
  <p id={statusId} data-upload-status class="text-xs text-text-muted mt-2"></p>

  <script>
    import { attachFotoUploadHandlers } from '../scripts/admin-editor';

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', attachFotoUploadHandlers, { once: true });
    } else {
      attachFotoUploadHandlers();
    }
  </script>
</div>
